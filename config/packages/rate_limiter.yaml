framework:
    rate_limiter:
        auth_request:
            policy: 'sliding_window'
            limit: 5
            interval: '15 minutes'
        auth_verify:
            policy: 'sliding_window'
            limit: 10
            interval: '15 minutes'
