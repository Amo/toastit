Toast actions processed

Toastit processed your email reply for "{{ toast.title }}".

Transfer: {{ transfer_applied ? 'done' : 'not applied' }}
Reword: {{ reword_applied ? 'done' : 'not applied' }}

{% if transfer_applied and workspace_suggestion %}
Transferred to: {{ workspace_suggestion.name }}
{% elseif workspace_suggestion %}
Suggested workspace: {{ workspace_suggestion.name }}
{{ workspace_suggestion.reason }}
{% endif %}

{% if reword_applied and proposed_title %}
Proposed title: {{ proposed_title }}

{{ proposed_description_text }}
{% endif %}
